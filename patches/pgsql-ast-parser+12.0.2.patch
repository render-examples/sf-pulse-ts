diff --git a/node_modules/pgsql-ast-parser/index.js b/node_modules/pgsql-ast-parser/index.js
index 2e90705..f393d15 100644
--- a/node_modules/pgsql-ast-parser/index.js
+++ b/node_modules/pgsql-ast-parser/index.js
@@ -1894,7 +1894,7 @@ function parse(sql, optEntry) {
     if (typeof optEntry !== 'string' && !Array.isArray(parsed)) {
         parsed = [parsed];
     }
-    return parsed;
+    return normalizePositionCalls(parsed);
 }
 exports.parse = parse;
 function parseArrayLiteral(sql) {
@@ -1966,6 +1966,31 @@ function _parse(sql, grammar, entry) {
         throw e;
     }
 }
+function normalizePositionCalls(node) {
+    var _a, _b, _c, _d;
+    if (Array.isArray(node)) {
+        return node.map(normalizePositionCalls);
+    }
+    if (!node || typeof node !== 'object') {
+        return node;
+    }
+    const normalizedEntries = Object.entries(node).map(([key, value]) => [
+        key,
+        normalizePositionCalls(value),
+    ]);
+    const normalized = Object.fromEntries(normalizedEntries);
+    if (normalized.type === 'call'
+        && ((_b = (_a = normalized.function) === null || _a === void 0 ? void 0 : _a.name) === null || _b === void 0 ? void 0 : _b.toLowerCase()) === 'position'
+        && ((_c = normalized.args) === null || _c === void 0 ? void 0 : _c.length) === 1
+        && ((_d = normalized.args[0]) === null || _d === void 0 ? void 0 : _d.type) === 'binary'
+        && normalized.args[0].op === 'IN') {
+        return Object.assign(Object.assign({}, normalized), { args: [
+                normalized.args[0].left,
+                normalized.args[0].right,
+            ] });
+    }
+    return normalized;
+}
 
 
 /***/ }),
@@ -4189,9 +4214,12 @@ const grammar = {
         { "name": "with_statement_bindings", "symbols": ["with_statement_binding", "with_statement_bindings$ebnf$1"], "postprocess": ([head, tail]) => {
                 return [head, ...(tail || [])];
             } },
-        { "name": "with_statement_binding", "symbols": ["word", (lexer_1.lexerAny.has("kw_as") ? { type: "kw_as" } : kw_as), "lparen", "with_statement_statement", "rparen"], "postprocess": x => (0, lexer_2.track)(x, {
+        { "name": "with_statement_binding$ebnf$1", "symbols": [], "postprocess": function(d) { return null; } },
+        { "name": "with_statement_binding$ebnf$1", "symbols": ["collist_paren"], "postprocess": id },
+        { "name": "with_statement_binding", "symbols": ["word", "with_statement_binding$ebnf$1", (lexer_1.lexerAny.has("kw_as") ? { type: "kw_as" } : kw_as), "lparen", "with_statement_statement", "rparen"], "postprocess": x => (0, lexer_2.track)(x, {
                 alias: asName(x[0]),
-                statement: unwrap(x[3]),
+                ...x[1] && { columnNames: x[1].map(asName) },
+                statement: unwrap(x[4]),
             }) },
         { "name": "with_statement_statement", "symbols": ["selection"] },
         { "name": "with_statement_statement", "symbols": ["insert_statement"] },
diff --git a/node_modules/pgsql-ast-parser/src/parser.ts b/node_modules/pgsql-ast-parser/src/parser.ts
index 313e51c..370ce15 100644
--- a/node_modules/pgsql-ast-parser/src/parser.ts
+++ b/node_modules/pgsql-ast-parser/src/parser.ts
@@ -63,7 +63,7 @@ export function parse(sql: string, optEntry?: string | ParseOptions): any {
     if (typeof optEntry !== 'string' && !Array.isArray(parsed)) {
         parsed = [parsed]
     }
-    return parsed;
+    return normalizePositionCalls(parsed);
 }
 
 export function parseArrayLiteral(sql: string): string[] {
@@ -140,3 +140,33 @@ function _parse(sql: string, grammar: Grammar, entry?: string): any {
         throw e;
     }
 }
+
+function normalizePositionCalls(node: any): any {
+    if (Array.isArray(node)) {
+        return node.map(normalizePositionCalls);
+    }
+    if (!node || typeof node !== 'object') {
+        return node;
+    }
+
+    const normalizedEntries = Object.entries(node).map(([key, value]) => [
+        key,
+        normalizePositionCalls(value),
+    ]);
+    const normalized = Object.fromEntries(normalizedEntries) as any;
+
+    if (normalized.type === 'call'
+        && normalized.function?.name === 'position'
+        && normalized.args?.length === 1
+        && normalized.args[0]?.type === 'binary'
+        && normalized.args[0]?.op === 'IN') {
+        return {
+            ...normalized,
+            args: [
+                normalized.args[0].left,
+                normalized.args[0].right,
+            ],
+        };
+    }
+    return normalized;
+}
diff --git a/node_modules/pgsql-ast-parser/src/syntax/expr.ne b/node_modules/pgsql-ast-parser/src/syntax/expr.ne
index aff8c67..ed21066 100644
--- a/node_modules/pgsql-ast-parser/src/syntax/expr.ne
+++ b/node_modules/pgsql-ast-parser/src/syntax/expr.ne
@@ -345,6 +345,5 @@ spe_substring -> (word {% kw('substring') %})
                 ...x[3] && {for: x[3][1]},
             }) %}
 
-
 various_binaries
     -> kw_at kw_time kw_zone {% () => 'AT TIME ZONE' %}
diff --git a/node_modules/pgsql-ast-parser/src/syntax/with.ne b/node_modules/pgsql-ast-parser/src/syntax/with.ne
index a9be4d4..e5b2c79 100644
--- a/node_modules/pgsql-ast-parser/src/syntax/with.ne
+++ b/node_modules/pgsql-ast-parser/src/syntax/with.ne
@@ -29,9 +29,10 @@ with_statement_bindings -> with_statement_binding (comma with_statement_binding
 } %}
 
 
-with_statement_binding -> word %kw_as lparen with_statement_statement rparen {% x => track(x, {
+with_statement_binding -> word collist_paren:? %kw_as lparen with_statement_statement rparen {% x => track(x, {
     alias: asName(x[0]),
-    statement: unwrap(x[3]),
+    ...x[1] && { columnNames: x[1].map(asName) },
+    statement: unwrap(x[4]),
 }) %}
 
 with_statement_statement
